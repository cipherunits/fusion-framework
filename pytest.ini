[pytest]
testpaths = tests/python
pythonpath = crates/fusion-py/python
addopts = -ra --strict-markers
markers =
    integration: tests that require a running HTTP server
    slow: long-running tests (opt-in)
